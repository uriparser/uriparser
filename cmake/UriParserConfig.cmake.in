# Compute paths
get_filename_component(URIPARSER_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(URIPARSER_INCLUDE_DIR "@CONF_INCLUDE_DIRS@" PARENT_SCOPE)

if(NOT TARGET uriparser AND NOT URIPARSER_BINARY_DIR)
  include("${URIPARSER_CMAKE_DIR}/UriParserTargets.cmake")
endif()

set(BUILD_SHARED_LIBS @BUILD_SHARED_LIBS@)

if(MSVC)
  if(BUILD_SHARED_LIBS)
    get_target_property(LIB_LIB_FILENAME @LIB_NAME@ IMPORTED_IMPLIB_@CONFIG_TYPE@)
  else(BUILD_SHARED_LIBS)
    get_target_property(LIB_LIB_FILENAME @LIB_NAME@ IMPORTED_LOCATION_@CONFIG_TYPE@)
  endif(BUILD_SHARED_LIBS)
else()
  get_target_property(LIB_LIB_FILENAME @LIB_NAME@ IMPORTED_LOCATION_@CONFIG_TYPE@)
endif()

set(URIPARSER_LIBRARY ${LIB_LIB_FILENAME} PARENT_SCOPE)
#compatiblity
set(URIPARSER_LIBRARIES ${LIB_LIB_FILENAME} PARENT_SCOPE)

set(URIPARSER_FOUND TRUE)
